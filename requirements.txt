# ============================================================
# ComfyUI Ultimate Colab — Requirements
# Python 3.12+
# ============================================================

# ── Core UI ──────────────────────────────────────────────────
rich>=13.7.0                  # Beautiful terminal output / dashboard
click>=8.1.7                  # CLI framework

# ── HTTP / Downloads ────────────────────────────────────────
requests>=2.31.0              # HTTP client
httpx>=0.27.0                 # Async HTTP client
aiohttp>=3.9.5                # Async HTTP (for parallel downloads)
tqdm>=4.66.4                  # Progress bars
aiofiles>=23.2.1              # Async file I/O

# ── HuggingFace ─────────────────────────────────────────────
huggingface-hub>=0.23.4       # HuggingFace Hub client

# ── Git ─────────────────────────────────────────────────────
gitpython>=3.1.43             # Git operations

# ── System / GPU ────────────────────────────────────────────
psutil>=5.9.8                 # System resource monitoring
py-cpuinfo>=9.0.0             # CPU info
gputil>=1.4.0                 # GPU usage (fallback)

# ── Data ────────────────────────────────────────────────────
pydantic>=2.7.0               # Data validation / settings
pydantic-settings>=2.3.0      # Settings management

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0.1          # .env file support
packaging>=24.1               # Version parsing
tomlkit>=0.12.5               # TOML read/write
filelock>=3.15.0              # File-based locking

# ── Testing (optional, used by dev extras) ──────────────────
# pytest>=8.2.2
# pytest-asyncio>=0.23.7
# pytest-cov>=5.0.0
# pytest-mock>=3.14.0
# responses>=0.25.3

# ── Dev tools (optional) ────────────────────────────────────
# black>=24.4.2
# ruff>=0.4.9
# mypy>=1.10.0
